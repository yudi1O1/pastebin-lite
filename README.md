## Pastebin Lite

Mini Pastebin-like app with TTL and view limits.

### Run locally
npm install
npm run dev

### Persistence
Uses Upstash Redis.

### Notes
Supports deterministic time using TEST_MODE and x-test-now-ms header.
